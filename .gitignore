# ambiente
.env
.env.*
!.env.example

# node / nest
node_modules/
dist/
coverage/
*.tsbuildinfo

# prisma (client gerado)
api/src/generated/

# flutter (gerado)
app/.dart_tool/
app/build/
app/.flutter-plugins*
app/.packages
app/android/key.properties
app/android/*.jks

# dados locais
uploads/

# cópias do contrato (fonte: api/openapi.json)
app/openapi.source.json
app/openapi.json

# l10n gerado (flutter gen-l10n via make app-gen)
app/lib/l10n/generated/

# IDE
app/*.iml

# dumps locais (make db-backup / uploads-backup)
backups/
