# api

API REST do portfólio (NestJS 12, Prisma 7, PostgreSQL). Spec de implementação
em [`../.specs/API.md`](../.specs/API.md); contrato em `../.specs/GLOBAL.md`.

Nada roda no host: use os alvos do `Makefile` na raiz (`make up`, `make api-test`,
`make api-e2e`, `make openapi`, …).
