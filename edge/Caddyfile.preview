# Serve app/build/web como em produção (precompressed, fallback SPA), atrás do
# edge de dev. Não faz TLS nem headers: isso é papel do edge.
:8080

root * /srv
try_files {path} /index.html
header Cache-Control "no-cache"
file_server {
	precompressed br gzip
}
